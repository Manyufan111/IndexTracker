# 指数分析工具需求文档（S&P 500 + 纳斯达克）

## 0. 已确认需求决策（2026-03-31）
- 纳斯达克指数口径：`Nasdaq Composite`。
- 数据频率：仅日线（Daily）。
- 更新策略：每次运行都强制尝试拉取最新数据。
- 预测时间范围：短期（1-5 交易日）+ 中期（1-4 周）+ 长期（1-3 个月）。
- 概率模型偏好：可解释的规则/统计模型优先。
- 输出形式：仅中文易读结论（首版不要求 JSON/表格扩展输出）。
- 数据源策略：免费优先，并且必须满足可靠性要求。
- 首版交付形态：本地 CLI 工具。

## 0.1 增量需求决策（2026-04-01）
- 在现有 CLI 基础上新增“简洁 UI 界面”用于展示分析结果。
- UI 必须包含：
  - S&P 500 与 Nasdaq Composite 趋势图；
  - 趋势图下方的走势分析文本；
  - 折叠面板（默认收起），展开后展示“数据来源 + 分析方法”。
- UI 目标：信息清晰、层级简洁、用户可快速查看关键结论。

## 0.2 概率模型增量决策（2026-04-01）
- `quantile` 模型目标：
  - 降低单边极端概率泛滥；
  - 通过时间序列 OOF 校准提高概率可靠性；
  - 支持回测评估与分层校准报告。
- 默认模型为 `quantile`（当前仅保留单模型链路）。

## 0.3 quantile 稳定性增量决策（2026-04-01）
- 保留 quantile 主框架（q10/q50/q90 + raw + OOF calibration + shrinkage）。
- 新增长期稳定性优化目标：
  - 降低 raw 与 calibrated 的过度翻转；
  - 强化 long horizon 的可解释性与自然度。
- 保留旧参数档位（`baseline`），新增优化档位（`optimized_v1`）用于 A/B。
- 新增诊断与实验输出能力：
  - horizon 级链路诊断（label/raw/cal/final、可靠性分桶、q/sigma/threshold）；
  - 消融实验表格输出（threshold/shrinkage/sigma_floor/calibration/pipeline）。

## 0.4 Horizon-Specific 链路决策（2026-04-01）
- 不再默认对所有 horizon 使用统一 full chain。
- 临时默认配置：
  - `short = raw + calibration + shrinkage`；
  - `mid = raw only`（默认关闭 calibration）；
  - `long = raw only`（默认关闭 calibration）。
- `mid/long` 的 calibration 仅作为实验链路，不再作为默认推理链路。

## 0.5 输出口径决策（2026-04-01，修订）
- 三分类（`up/flat/down`）继续作为主模型与主评估链路。
- 二分类只作为“方向展示层”，不可直接替代三分类主链路。
- 展示层必须保留“不确定区”语义（可映射自 `flat`），用户可见口径为：`上涨 / 不确定 / 下跌`。
- 展示层方向概率默认禁止极端值（默认上限 `0.90`，可配置），超过上限时必须压缩并记录告警。
- 保留 horizon-specific 默认链路：`short=full_chain`，`mid=raw_only`，`long=raw_only`。

## 1. 项目背景
用户需要一个面向投资分析的工具，能够对 **S&P 500 指数** 与 **纳斯达克指数（本期固定为 Nasdaq Composite）** 做历史与当前行情分析，并给出清晰、可理解的趋势判断。  
核心要求是：分析时必须优先使用最新可用数据，而不是只基于离线历史数据。

## 2. 项目目标
实现一个可运行的分析工具，支持：
- 拉取并更新最新指数数据；
- 结合历史与当前数据输出趋势分析；
- 给出多个未来走势场景及其发生概率；
- 用简单易懂的语言生成结论，便于非专业用户阅读。

## 3. 用户与使用场景
- 目标用户：希望快速理解美股主要指数走势的普通投资者或研究者。
- 典型场景：
  - 每日查看当前趋势与短中期可能方向；
  - 对比 S&P 500 与纳斯达克强弱；
  - 参考概率结论做进一步决策（非自动交易）。

## 4. 范围定义
### 4.1 本期范围（In Scope）
- 指数范围：S&P 500、Nasdaq Composite。
- 数据粒度：仅日线数据（Daily OHLCV）。
- 时间范围：支持历史回溯分析（如 1M/3M/6M/1Y/3Y/5Y）+ 当前最新数据分析。
- 输出内容：
  - 当前趋势判断；
  - 未来场景（上涨/震荡/下跌）概率（覆盖短中长期）；
  - 中文简洁自然语言结论；
  - 风险提示与不确定性说明。
- 使用方式：CLI + 轻量 Web UI（新增）。
- UI 展示要求：
  - 页面展示两个指数趋势图（SP500、NASDAQ）；
  - 每个趋势图下展示对应走势分析；
  - 页面包含折叠面板，展开后显示“数据来源、分析方法”。

### 4.2 非本期范围（Out of Scope）
- 自动下单或实盘交易。
- 个股级别深度基本面分析。
- 复杂衍生品定价与高频策略。
- 投资收益承诺。
- 复杂交互式可视化工作台（多页路由、拖拽编辑等）。

## 5. 功能需求
### FR-1 数据获取与更新
- 系统应支持从至少一个可靠行情源拉取指数数据（可配置数据源）。
- 系统应在每次分析前执行“最新数据检查与更新”。
- 数据源策略应为“免费优先 + 可靠性优先”。
- 当主数据源不可用时，应提示失败原因，并回退到最近一次可用数据（如存在）。
- 数据字段至少包含：时间戳、开高低收、成交量（若源支持）。

### FR-1.1 数据源可靠性要求（重点）
- 数据源应具备稳定可访问接口与明确字段定义，避免依赖高风险临时抓取方案。
- 返回数据必须包含可追溯时间信息（交易日期/更新时间），用于判断是否为最新数据。
- 系统应在分析前执行“新鲜度校验”，发现数据滞后时明确提示。
- 系统应保留最近一次成功拉取记录，用于故障回退与问题排查。

### FR-2 历史与当前联合分析
- 系统应基于历史窗口与当前日线数据共同分析：
  - 趋势方向（短期/中期/长期）；
  - 波动性水平；
  - 动量变化。
- 系统应覆盖以下预测窗口：
  - 短期：1-5 交易日；
  - 中期：1-4 周；
  - 长期：1-3 个月。
- 分析结果应支持分别查看 S&P 500 与纳斯达克，并可给出对比结论。

### FR-3 趋势场景概率输出
- 系统应输出三个用户可见场景：
  - 上涨趋势延续；
  - 不确定（震荡/方向不足）；
  - 下跌回撤。
- 每个场景应给出概率值（0-100%），且三者总和为 100%（允许四舍五入误差 ±0.5%）。
- 其中“不确定”可由主模型 `flat` 概率或展示层降置信规则共同决定。
- 系统应在结果中标注概率为模型估计，不构成投资建议。

### FR-4 结论可读性
- 系统应生成“简明结论”模块，使用中文非技术化语言说明：
  - 现在处于什么走势；
  - 接下来最可能发生什么；
  - 需要重点关注的风险信号。
- 结论应控制在可快速阅读的长度（建议 3-8 句）。

### FR-5 参数与配置
- 支持用户指定：
  - 分析指数（SP500 / NASDAQ / BOTH）；
  - 分析周期（如 1M、3M、6M、1Y）；
  - 输出语言（首版固定中文）。
- 支持配置数据源凭证（如 API Key）与默认时区。

### FR-6 异常处理
- 网络错误、数据缺失、字段异常时应给出明确报错与可执行建议。
- 当最新数据不可得时，应明确标注“当前结果基于最近一次成功更新数据”，并显示该时间戳。

### FR-7 UI 趋势图展示（新增）
- 系统应提供一个简洁 UI 页面，展示：
  - S&P 500 趋势图；
  - Nasdaq Composite 趋势图。
- 趋势图应至少支持近 1M/3M/6M/1Y 视图切换（或通过参数切换）。
- 图表数据应与分析计算使用同一数据快照，避免图文不一致。

### FR-8 UI 分析文本与折叠信息（新增）
- 每个趋势图下方应展示对应“走势分析结论”。
- 页面应提供折叠区域（Accordion/Details）：
  - 默认收起；
  - 展开后显示“数据来源（Provider + 更新时间）”；
  - 展开后显示“分析方法（特征/评分/概率模型简述）”。
- 折叠信息内容应使用用户可读语言，避免过度技术术语堆叠。

### FR-9 概率模型可靠性（新增）
- 系统应支持 horizon=`5/20/60` 交易日三分类概率建模。
- 每个 horizon 输出 `up/flat/down`，且三者和为 `1`（展示层转百分比）。
- 概率应支持时间序列 OOF 校准流程；当某 horizon 在最新诊断中确认 calibration 劣化时，允许该 horizon 默认关闭 calibration（需保留对照实验与可解释日志）。
- 系统应提供概率收缩机制与单边上限约束（默认上限 0.72，极端 regime 可放宽）。
- 系统应新增“方向展示层”独立校准（binary calibration）并输出 raw/calibrated/final 对比，禁止直接复用三分类校准结果后硬映射。
- 系统应具备展示层 sanity 规则：
  - 中长期出现相反且极端结论时触发降置信；
  - 在明显 regime shift 下压缩过高方向置信度。

### FR-10 quantile 诊断与实验能力（新增）
- 系统应输出每个 horizon 的诊断信息：
  - 训练集与 OOF 验证集标签分布（up/flat/down）；
  - 概率链路（raw/calibrated/final）；
  - 分布统计（q10/q50/q90、sigma、threshold）；
  - 评估信息（log loss/brier/accuracy/confusion）；
  - 校准信息（prob_up/prob_down reliability bins）。
- 系统应支持消融实验输出（表格或日志），至少覆盖：
  - threshold 宽度对比；
  - shrinkage 强度对比；
  - sigma_floor 灵敏度对比；
  - calibration 方案对比；
  - final 链路对比。
- 消融实验应支持 horizon-specific 输出与推荐（short/mid/long 分开评估）。
- 系统应提供参数档位切换能力，用于 baseline 与优化方案 A/B 对照。

## 6. 非功能需求
### NFR-1 数据时效性
- “最新数据优先”是核心约束：每次运行默认先尝试拉取最新行情。
- 分析输出必须展示“数据最后更新时间（含时区）”。

### NFR-2 性能
- 常规一次分析在网络正常情况下建议 <= 10 秒（不含首次冷启动依赖安装）。

### NFR-3 可维护性
- 代码结构模块化（数据获取、分析引擎、结果生成分层）。
- 核心参数可配置，不硬编码在业务逻辑中。

### NFR-4 可观测性
- 至少具备基础日志：数据拉取、分析开始/结束、错误信息。

### NFR-5 安全与合规
- 不在仓库明文存储 API Key。
- 输出中应包含“仅供参考，非投资建议”声明。

### NFR-6 数据可靠性
- 首版选用“免费且稳定”的行情源，要求历史数据连续性良好、字段稳定。
- 每次运行需记录数据源名称与拉取状态，便于追踪可靠性问题。
- 当数据源异常时，需在输出中明确标注“本次结论可能受数据时效影响”。

### NFR-7 UI 可用性（新增）
- 页面首屏应可快速定位核心信息（趋势图 + 结论）。
- 折叠区域默认收起，避免干扰主阅读路径。
- UI 在常见桌面分辨率下应保持布局清晰，不出现关键信息遮挡。

## 7. 输出规范（用户可见）
- 输出至少包含以下模块：
  - 数据状态：数据源、最后更新时间、覆盖时间范围；
  - 指数分析：S&P 500、纳斯达克分别结论；
  - 概率场景：上涨/不确定/下跌及概率；
  - 综合结论：简明建议性描述（非交易指令）；
  - 风险提示：模型局限与不确定性。
- UI 额外模块（新增）：
  - 趋势图展示区（SP500 + NASDAQ）；
  - 图下分析区（对应指数的走势结论）；
  - 折叠说明区（数据来源 + 分析方法）；
  - 概率细节区（raw vs calibrated、horizon threshold、regime、confidence）。

## 8. 技术方向（约束与建议）
- 开发语言：Python（生态完善、数据分析库成熟，适合快速实现 CLI + 分析流程）。
- 架构建议：
  - `data_provider`：行情拉取与更新；
  - `analyzer`：指标/趋势/概率计算；
  - `reporter`：自然语言结论生成；
  - `cli`：命令行入口。
- 未来可扩展：
  - 更丰富的 Web UI 交互（多页/筛选/对比）；
  - 定时任务与日报；
  - 多数据源容灾；
  - 更多指数与宏观指标。

## 9. 验收标准（Definition of Done）
- 可在本地一条命令运行并输出分析报告。
- 能成功分析 S&P 500 与纳斯达克，且结果包含概率场景。
- 概率输出覆盖短期（1-5 交易日）、中期（1-4 周）、长期（1-3 个月）三个时间尺度。
- 每次运行默认尝试拉取最新数据，并在输出中显示更新时间。
- 当数据不可更新时，系统会清晰告警并标注回退数据时间。
- 输出为中文易读结论，普通用户可快速理解核心信息。
- 提供可访问的简洁 UI 页面，包含 SP500 与 NASDAQ 趋势图。
- 趋势图下方存在对应走势分析文本，且图文数据时间一致。
- 折叠区域默认收起，展开后可见“数据来源 + 分析方法”。
- 在图下分析区可见每个 horizon 的 `raw vs calibrated` 概率、阈值、regime 与 confidence。
- 默认模型为 `quantile`，并作为唯一可运行模型。

## 10. 已确认实现约束
- 当前阶段由“仅 CLI”升级为“CLI + 轻量 UI”。
- 模型优先采用可解释统计/规则方法，不以黑盒高复杂模型为首要目标。
- 数据源选择以“免费 + 可靠”为第一原则，必须提供可追溯的更新时间信息。
- 当前模型策略为“单轨制”：仅保留 `quantile` 主链路。
